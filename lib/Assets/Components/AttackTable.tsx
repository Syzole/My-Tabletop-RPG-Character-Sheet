import DnDCharacter from "@/lib/DnDCharacter";
import { Weapon } from "@/lib/types";
import { useState, useEffect } from "react";

export default function AttackTable({ character }: { character: DnDCharacter }) {
    // first go through charecter equipped weapons and create a table row for each
}